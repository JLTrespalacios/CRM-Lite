{
  "name": "crm-citas-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install"
  },
  "engines": {
    "node": "24.x"
  }
}